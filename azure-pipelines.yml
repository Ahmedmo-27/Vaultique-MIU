trigger:
- main

pool:
  name: Default  # This must match the name of the pool where your self-hosted agent is registered

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '20.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'npm install and build'
