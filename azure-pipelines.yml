trigger:
- main

pool:
  name: Default  # Replace with your agent pool name if different

steps:
- script: |
    node -v
    npm install
    npm run build
  displayName: 'Use local Node.js and build'
